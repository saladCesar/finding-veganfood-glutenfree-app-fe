import React from "react";
import { Link } from "react-router-dom";
import { Button } from "react-bootstrap";

export const Card = () => {
	return (
        <div>
            <h1>Similiar to Star Wars project, each recipe, restaurant, educationm etc will be presented on a card. and this will be the building block to call it.</h1>
        </div>
    );
};
